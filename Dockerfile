# The base image to be used is  python:3.7.7-alpine
# The app is located in /app
# The app runs on port 5000

# The following commands are used to start the app
# pip3 install --upgrade pip
# pip3 install -r requirements.txt
# python3 app.py
# ============ Contents of Dockerfile ============
